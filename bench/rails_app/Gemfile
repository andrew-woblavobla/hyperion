# Intentionally empty. The Rails-bench rackups load Rails via
# bench/Gemfile.4way (BUNDLE_GEMFILE), not via this Gemfile.
# This file exists only so `rails` doesn't complain when invoked
# from inside bench/rails_app/.
source 'https://rubygems.org'
